

function wait(seconds){

    return new Promise(resolve =>{

         setTimeout(() => resolve() , 1000*seconds);
    });
}

const Operations = async () =>{

    const waiting_for_response = [];

    for(let i = 0 ; i< 5 ; i++){
        
    }
}