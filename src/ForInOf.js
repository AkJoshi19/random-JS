/* let animals = ['HEN', 'PIG', 'FISH', 'Rabit'];
let names = ['Gertrude', 'Henry', 'Melvin', 'Billy Bob'];

for (let animal of animals) {
 
  console.log( `${animal}`);
}    */

let str = 'abcde';

for (let char of str) {
  
  console.log(char.toUpperCase().repeat(3));
}